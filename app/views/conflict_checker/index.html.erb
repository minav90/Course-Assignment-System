<h1>Conflict Checker</h1>
<p>The following list shows groups of courses that had the same preference # 1 combination chosen by multiple faculty:</p>

<table>
  <thead>
    <tr>
      <th>Faculty Course</th>
      <th>Preference # 1</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 
  <tbody>
    <% @conflicts.each do |conflict| %>
      <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
        <td><%= conflict.faculty_course_id %></td>
        <td><%= conflict.preference1_id %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 
<br>


